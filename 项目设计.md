你是一名资深全栈工程师、AI Agent 工程师和桌面应用架构师。

请从零实现一个完整可运行的桌面应用项目：

# Job Agent Desktop

这是一个“网页版招聘平台求职 Agent 客户端”。

核心目标：

1. 用户可以直接在桌面客户端内部浏览网页版招聘网站。

2. Agent 可以读取当前招聘网页中的职位、公司、JD、HR 消息等信息。

3. Agent 可以判断岗位是否适合用户。

4. Agent 可以在规则允许的情况下执行投递、发起沟通等操作。

5. Agent 可以读取 HR 消息，并生成回复。

6. 部分低风险消息允许自动回复。

7. 中高风险消息必须在客户端弹出确认卡片，由用户确认、修改或拒绝。

8. 用户可以随时暂停 Agent 并手动接管网页。

9. 所有 Agent 行为必须可观察、可追踪、可恢复。

10. 项目使用 LangChain + LangGraph 构建 Agent 和工作流。

不要把它实现成一个简单的 Selenium/Playwright 自动点击脚本。

它应该是一个真正具有桌面产品形态的：

“招聘 Browser + Agent Copilot + 自动化 Workflow”。

---

# 一、总体技术栈

前端桌面客户端：

- Electron

- React

- TypeScript

- Vite

- Tailwind CSS

- shadcn/ui

- Zustand

- TanStack Query

内嵌浏览器：

- Electron WebContentsView

Agent 后端：

- Python 3.12+

- FastAPI

- LangChain

- LangGraph

- Pydantic

- SQLAlchemy

- SQLite

调试：

- LangSmith 可选支持

- Python logging / structlog

- Electron 日志

测试：

- pytest

- Playwright 用于 Browser Adapter 自动化测试

- React 测试按需加入

Python 包管理：

- uv

Node 包管理：

- pnpm

第一版只运行在：

Windows 10 / Windows 11

---

# 二、总体架构

系统分成两个主要部分：

```text
┌──────────────────────────────────────────────┐
│              Electron Desktop               │
│                                              │
│ React UI             WebContentsView         │
│    │                       │                 │
│    │                 招聘网站网页             │
│    │                       │                 │
│    └──── Electron Main ─────┘                │
│                 │                            │
│         Browser Controller                   │
└─────────────────┬────────────────────────────┘
                  │
          HTTP + WebSocket
                  │
                  ↓
┌──────────────────────────────────────────────┐
│              Python Backend                  │
│                                              │
│ FastAPI                                      │
│ LangChain                                    │
│ LangGraph                                    │
│                                              │
│ Job Agent                                    │
│ Chat Agent                                   │
│ Workflow                                     │
│ Rules                                        │
│ Persistence                                  │
│ Database                                     │
└──────────────────────────────────────────────┘
```

客户端负责：

- UI

- 浏览器

- 网页操作

- 用户交互

- Agent 状态显示

- Human-in-the-loop

Python 后端负责：

- Agent 推理

- LangGraph 工作流

- 职位判断

- 消息理解

- 回复生成

- 规则判断

- 数据持久化

- 任务状态

- Agent 配置

---

# 三、最重要的架构原则

必须严格遵守以下原则。

## 1. Agent 不允许直接操作 CSS Selector

禁止让 LLM 输出：

```json
{
  "selector": ".xxx > div:nth-child(3)",
  "action": "click"
}
```

更禁止：

```json
{
  "x": 1320,
  "y": 810
}
```

Agent 只能产生语义级操作，例如：

```json
{
  "action": "APPLY_JOB"
}
```

或者：

```json
{
  "action": "SEND_MESSAGE",
  "message": "您好，我对这个岗位比较感兴趣。"
}
```

---

## 2. Platform Adapter 负责网页细节

Agent 不知道 DOM。

LangGraph 不知道 DOM。

业务代码不直接操作 Selector。

结构必须是：

```text
Agent
↓
Workflow
↓
Platform Service
↓
Platform Adapter
↓
Browser Controller
↓
Electron WebContents
↓
Website
```

---

## 3. Browser 层必须抽象

定义统一接口：

```typescript
interface BrowserController {
    getUrl(): Promise<string>;

    navigate(url: string): Promise<void>;

    back(): Promise<void>;

    forward(): Promise<void>;

    reload(): Promise<void>;

    getPageTitle(): Promise<string>;

    getText(selector: string): Promise<string>;

    getTexts(selector: string): Promise<string[]>;

    click(selector: string): Promise<void>;

    fill(selector: string, text: string): Promise<void>;

    waitFor(selector: string, timeout?: number): Promise<boolean>;

    evaluate<T>(script: string): Promise<T>;

    screenshot(): Promise<string>;
}
```

正式环境：

```text
ElectronBrowserController
```

测试环境未来允许：

```text
PlaywrightBrowserController
```

上层 Adapter 不应该依赖具体浏览器实现。

---

# 四、Electron 客户端

Electron 是本项目的主要用户界面。

不要使用旧的 BrowserView。

不要优先使用 `<webview>`。

使用：

```text
WebContentsView
```

加载招聘网站。

---

# 五、主界面设计

整体布局：

```text
┌──────────────────────────────────────────────────────────────────┐
│ Job Agent     ● Agent Running               [Pause]      ⚙       │
├────────┬────────────────────────────────────────┬────────────────┤
│        │ Browser Toolbar                        │ Agent          │
│        ├────────────────────────────────────────┤ Panel          │
│ 导航   │                                        │                │
│        │                                        │ 当前职位       │
│ Browser│                                        │                │
│        │          Recruitment Website           │ 匹配分析       │
│ Jobs   │                                        │                │
│        │                                        │ Agent 建议     │
│ Chat   │                                        │                │
│        │                                        │ 操作           │
│ Review │                                        │                │
│        │                                        │ [Apply]        │
│ History│                                        │ [Skip]         │
│        │                                        │                │
│ Config │                                        │                │
├────────┴────────────────────────────────────────┴────────────────┤
│ Agent status / workflow / logs                                   │
└──────────────────────────────────────────────────────────────────┘
```

---

# 六、客户端页面

至少实现：

## Browser

主招聘网站浏览器。

功能：

- 返回

- 前进

- 刷新

- 地址显示

- 页面标题

- Agent 当前状态

- Pause / Resume

---

## Jobs

显示 Agent 已经发现和分析过的职位。

字段：

- 公司

- 岗位

- 城市

- 薪资

- 匹配分

- 状态

- 是否投递

- 创建时间

支持：

- 搜索

- 状态过滤

- 打开网页

- 查看 Agent 分析

---

## Chat

显示所有 HR 会话。

字段：

- 公司

- HR

- 最后一条消息

- unread

- Agent 判断

- 当前状态

---

## Review

这是非常重要的页面。

展示所有：

```text
WAITING_USER_CONFIRMATION
```

任务。

例如：

```text
腾讯 HR

HR：
明天下午方便面试吗？

Agent 建议：

您好，明天下午三点以后都方便，请问具体面试时间是什么时候？

[确认发送]

[编辑后发送]

[拒绝]

[稍后处理]
```

---

## History

展示：

- Agent 操作历史

- 投递历史

- 回复历史

- 错误

- Workflow 状态

---

## Settings

后面详细说明。

---

# 七、Agent Side Panel

在 Browser 页面右侧始终存在 Agent 面板。

如果当前是职位详情页：

展示：

```text
职位

Go 后端开发工程师

公司：
XXX

薪资：
20-30K

地点：
深圳


匹配度
86 / 100


匹配：

✓ Go
✓ MySQL
✓ Redis
✓ Docker
✓ 深圳


风险：

⚠ 要求 3 年经验


Agent 判断：

岗位技术栈高度匹配，但是经验要求略高。


建议：

APPLY


[立即投递]

[跳过]

[收藏]
```

---

如果当前为聊天页：

显示：

```text
当前 HR 消息：

什么时候方便面试？


Agent Intent：

INTERVIEW_TIME


Risk：

CONFIRM


Agent 建议回复：

您好，我明天下午三点后都有时间。


[发送]

[修改]

[不回复]
```

---

# 八、运行模式

顶部提供三种模式：

```text
Manual
Copilot
Agent
```

## Manual

Agent：

- 可以读取网页

- 可以分析

- 可以给建议

但：

不允许执行任何网页操作。

---

## Copilot

允许 Agent：

- 打开职位

- 浏览

- 分析

- 搜索

- 填写非关键内容

但以下操作必须确认：

- 投递

- 发消息

- 提交

---

## Agent

允许低风险操作自动执行。

但是以下操作必须 Human-in-the-loop：

- 面试时间

- 薪资

- Offer

- 入职时间

- 合同

- 重要承诺

- Agent 不确定的消息

---

# 九、Pause / Human Takeover

这是强制要求。

客户端顶部必须始终提供：

```text
Pause Agent
```

点击之后：

Agent 必须立即停止：

- 点击

- 输入

- 页面跳转

- 发送消息

- 投递

但是：

网页仍然完全可以由用户手动操作。

用户可以手动：

- 点击

- 浏览

- 回复

- 登录

随后：

```text
Resume Agent
```

Agent 必须重新读取当前页面。

不要假设网页还停留在暂停前状态。

设计：

```text
PAUSE
↓
USER TAKEOVER
↓
RESUME
↓
REFRESH PAGE STATE
↓
RECONCILE
↓
CONTINUE
```

---

# 十、LangChain

LangChain 负责：

- LLM abstraction

- Prompt

- Structured Output

- Agent tool definition

- Model switching

- Message model

真正需要 Agent 的模块第一版只有两个：

```text
JobAgent

ChatAgent
```

不要建立大量无意义 Agent。

---

# 十一、JobAgent

输入：

```text
Job
+
UserProfile
+
JobPreference
```

输出必须为 Pydantic Structured Output。

例如：

```python
class JobDecision(BaseModel):

    decision: Literal[
        "apply",
        "skip",
        "review"
    ]

    match_score: int

    strengths: list[str]

    concerns: list[str]

    reason: str
```

JobAgent 负责：

- 判断岗位是否匹配

- 给出匹配度

- 提取优点

- 提取风险

- 建议是否投递

JobAgent：

不得操作浏览器。

---

# 十二、Rule Engine

在调用 JobAgent 之前必须有 Rule Engine。

不要所有职位都调用 LLM。

例如：

```text
发现 1000 个职位

↓

Hard Rule

↓

剩余 250

↓

LLM

↓

推荐 80
```

Hard Rules：

- 工作地点

- 薪资

- 黑名单公司

- 黑名单岗位

- 学历

- 工作类型

- 用户明确排除项

规则应该可配置。

---

# 十三、ChatAgent

输入：

```text
当前 HR 消息

+

聊天上下文

+

Job 信息

+

UserProfile
```

输出：

```python
class ReplyDecision(BaseModel):

    intent: MessageIntent

    risk: Literal[
        "auto",
        "confirm",
        "manual"
    ]

    reply: str | None

    reason: str
```

Intent 至少包含：

```text
GREETING

RESUME_REQUEST

LOCATION

GRADUATION

EDUCATION

TECH_STACK

INTERVIEW_TIME

SALARY

ARRIVAL_DATE

OFFER

CONTRACT

OTHER
```

---

# 十四、消息 Risk

默认策略：

## AUTO

例如：

- 普通问候

- 是否还在找工作

- 学历

- 毕业年份

- 简历请求

- 技术栈

- 当前城市

允许 Agent 自动回复。

---

## CONFIRM

例如：

- 面试时间

- 到岗时间

- 薪资

- 工作地点调整

- 技术面试承诺

必须 Human-in-the-loop。

---

## MANUAL

例如：

- Offer

- 合同

- 签约

- 薪资谈判

- 确认入职

- 违约相关

- Agent 无法判断

Agent 只能提示用户。

不能自动发送。

所有分类规则必须允许在 Settings 中修改。

---

# 十五、LangGraph

使用 LangGraph 编排系统 Workflow。

不要建立一个超级大 Graph。

拆成：

```text
JobDiscoveryGraph

ApplicationGraph

ChatGraph
```

---

# 十六、JobDiscoveryGraph

大致：

```text
START

↓

fetch_jobs

↓

deduplicate

↓

hard_filter

↓

extract_job_detail

↓

evaluate_job

↓

route_decision

├─ SKIP
│
├─ REVIEW
│
└─ APPLY_CANDIDATE

↓

persist

↓

END
```

---

# 十七、ApplicationGraph

```text
START

↓

load_job

↓

verify_not_applied

↓

open_job

↓

verify_page

↓

contact_hr

↓

verify_chat

↓

generate_greeting

↓

send_greeting

↓

persist_application

↓

END
```

所有关键操作必须：

```text
ACTION
↓
VERIFY
```

禁止：

```text
click
sleep
click
sleep
```

---

# 十八、ChatGraph

```text
START

↓

load_message

↓

classify_message

↓

generate_reply

↓

risk_check

├─ AUTO
│    ↓
│   send
│
├─ CONFIRM
│    ↓
│ interrupt
│    ↓
│ send
│
└─ MANUAL
     ↓
   notify

↓

save_message

↓

END
```

---

# 十九、LangGraph Human-in-the-loop

必须使用：

```python
interrupt()
```

实现用户审批。

客户端通过 WebSocket 收到：

```json
{
    "type": "approval_required",
    "task_id": "xxx",

    "data": {
        "hr_message": "...",
        "suggested_reply": "..."
    }
}
```

客户端显示 ApprovalCard。

用户可以：

```text
Approve

Edit + Approve

Reject
```

之后后端：

```python
Command(
    resume={...}
)
```

继续执行 Graph。

---

# 二十、Interrupt 安全要求

任何可能产生副作用的操作：

```text
send_message

apply_job

contact_hr
```

不要写在 interrupt 前。

错误：

```text
send_message()

interrupt()
```

因为恢复时 Node 可能重新运行。

必须：

```text
generate_reply_node

↓

approval_node

↓

send_message_node
```

保持副作用节点独立。

同时对以下操作实现幂等保护：

- 投递

- 发送消息

- 联系 HR

数据库中记录 operation_id。

---

# 二十一、Graph State

State 只允许保存业务状态。

可以：

```python
class ApplicationState(TypedDict, total=False):

    job_id: str

    platform: str

    job: dict

    decision: dict

    status: str

    hr_message: str

    reply: str

    risk: str

    retry_count: int

    last_error: str | None

    requires_confirmation: bool
```

禁止：

```text
Playwright page

Electron object

Browser object

Database connection

LLM instance
```

进入 Graph State。

---

# 二十二、Platform Adapter

创建：

```text
platforms/
```

定义：

```python
class RecruitmentPlatform(ABC):

    async def get_jobs(...):
        ...

    async def get_job_detail(...):
        ...

    async def open_job(...):
        ...

    async def contact_hr(...):
        ...

    async def send_message(...):
        ...

    async def get_messages(...):
        ...

    async def send_resume(...):
        ...
```

第一版只实现一个平台 Adapter。

但必须设计为未来可以增加：

```text
BossAdapter

LiepinAdapter

ZhaopinAdapter

NowcoderAdapter
```

不要把所有平台逻辑写死在 Workflow。

---

# 二十三、Selector 隔离

平台目录：

```text
platforms/
└── boss/
    ├── adapter.py
    ├── parser.py
    ├── selectors.py
    └── models.py
```

DOM Selector 只能出现在：

```text
selectors.py
```

或者 Browser Adapter 层。

如果招聘网站页面改版，应尽量只需要修改 selectors/parser。

---

# 二十四、Browser RPC

Python 后端不能直接持有 Electron WebContents 对象。

Electron 和 Python 使用 WebSocket 双向通信。

实现 Browser RPC。

Python 发：

```json
{
    "type": "browser_command",

    "request_id": "uuid",

    "command": "GET_JOB_DETAIL",

    "payload": {}
}
```

Electron：

```text
收到命令

↓

调用 BrowserController

↓

执行 DOM 操作

↓

返回结果
```

例如：

```json
{
    "type": "browser_result",

    "request_id": "uuid",

    "success": true,

    "data": {}
}
```

必须支持 timeout。

必须支持 request_id。

必须支持错误返回。

---

# 二十五、FastAPI

至少提供：

```text
GET /health


POST /agent/start

POST /agent/pause

POST /agent/resume


GET /agent/state


GET /jobs

GET /jobs/{id}


GET /applications


GET /messages


GET /reviews


POST /reviews/{id}/approve

POST /reviews/{id}/reject


GET /config

PUT /config
```

---

# 二十六、WebSocket

提供：

```text
/ws
```

用于：

- Agent State

- Agent Logs

- Browser RPC

- Approval Required

- Job Analysis

- Message Analysis

- Error

- Workflow Event

定义统一消息协议：

```typescript
type WSMessage =
    | AgentStateEvent
    | BrowserCommand
    | BrowserResult
    | ApprovalRequired
    | WorkflowEvent
    | LogEvent
    | ErrorEvent
```

Python 端也使用对应 Pydantic Model。

---

# 二十七、数据库

第一版：

SQLite

ORM：

SQLAlchemy 2.x

至少包含：

```text
jobs

applications

companies

messages

conversations

agent_decisions

approval_tasks

agent_events

settings
```

---

# 二十八、Job Status

不要只有：

```text
applied = true
```

建立状态：

```text
DISCOVERED

PARSED

FILTERED

SKIPPED

REVIEW

APPROVED

CONTACTING

CONTACTED

WAITING_REPLY

HR_REPLIED

INTERVIEW

REJECTED

OFFER

ERROR
```

---

# 二十九、Agent Event Log

每个 Agent 行为必须写 event。

例如：

```json
{
    "event": "JOB_EVALUATED",

    "job_id": "xxx",

    "result": "APPLY",

    "score": 87,

    "time": "..."
}
```

操作网页：

```json
{
    "event": "CONTACT_HR",

    "job_id": "xxx",

    "success": true
}
```

---

# 三十、配置系统

Settings 中实现：

## Job Preference

```text
目标岗位

目标城市

最低薪资

学历

毕业年份

期望方向

排除岗位

公司黑名单
```

---

## Skills

用户可以设置技能和熟练度：

```text
Go

MySQL

Redis

Docker

Kafka

微服务

分布式
```

---

## Apply Policy

```text
是否开启自动投递

最低匹配分

每日最大投递量

投递间隔

是否允许 Agent 主动联系 HR
```

不要设计成无限制高频批量操作。

提供合理节流。

---

## Chat Policy

针对每类 Intent：

```text
AUTO

CONFIRM

MANUAL
```

用户可以自行调整。

---

## Model

允许配置：

```text
Model Provider

Model Name

API Key

Temperature

Timeout
```

API Key 不允许保存在代码库。

---

# 三十一、Secrets

使用：

```text
.env
```

或者系统安全存储。

至少：

```text
OPENAI_API_KEY
LANGSMITH_API_KEY
```

`.gitignore`：

```text
.env

browser-data/

auth.json

*.db

logs/
```

不要提交任何：

- Cookie

- Session

- API Key

- 登录凭据

---

# 三十二、安全

Electron 外部招聘网页属于不可信内容。

必须：

```typescript
nodeIntegration: false
contextIsolation: true
sandbox: true
```

禁止招聘网页直接：

- 使用 Node

- 调用 Python

- 使用 Electron API

- 访问本地文件

结构：

```text
Recruitment Website
↓
isolated WebContentsView

↓

Electron Main

↓

controlled IPC
```

Renderer 与 Electron Main 也通过严格 preload API 通信。

不要暴露：

```text
ipcRenderer
```

完整对象给网页。

---

# 三十三、导航限制

WebContentsView：

限制危险 navigation。

对于：

```text
window.open
```

做显式处理。

只允许必要招聘网站域名。

外部未知 URL：

使用系统浏览器打开或者阻止。

---

# 三十四、验证码

如果检测：

```text
验证码

人机验证

登录失效

异常安全验证
```

Agent 必须：

```text
PAUSE
```

客户端提示：

```text
需要人工完成验证
```

禁止尝试绕过验证码。

完成后用户点击：

```text
Resume
```

Agent 再重新读取页面。

---

# 三十五、错误恢复

每个 Browser Action：

```text
ACTION

↓

VERIFY
```

如果失败：

第一次：

```text
retry
```

第二次：

```text
refresh page state
```

仍失败：

```text
ERROR
```

保存：

- 当前 URL

- Page Title

- Screenshot

- Action

- Error

- Job ID

- Workflow ID

---

# 三十六、截图

截图主要用于：

Debug。

不是 OCR。

错误目录：

```text
logs/
2026-xx-xx/
timestamp-action-error.png
```

---

# 三十七、OCR

第一版不要实现 OCR。

第一版完全使用：

```text
DOM
+
JS
+
Browser Controller
```

未来如果遇到 Canvas / 图片内容再扩展 OCR。

---

# 三十八、Playwright

Playwright 第一版主要用于：

- Browser Adapter integration test

- 网站解析测试

- selector debug

- mock 测试

不要同时让 Playwright Chrome 和 Electron WebContents 控制同一个生产页面。

生产客户端优先：

```text
Electron WebContentsView
```

---

# 三十九、前端 State

使用 Zustand 管：

```text
agentMode

agentStatus

currentJob

currentPage

pendingReviews

browserState

connectionState
```

服务器数据：

TanStack Query。

---

# 四十、UI 风格

整体风格：

现代开发者工具 / AI Copilot。

要求：

- 深色 / 浅色模式

- 简洁

- 高信息密度

- 不花哨

- 清晰状态标签

- 明确危险操作

- 确认卡片突出

参考：

```text
VS Code
Cursor
Linear
ChatGPT Desktop
```

但不要直接复制任何产品。

---

# 四十一、第一版 MVP

不要一次实现全部功能。

按照以下阶段开发。

## Phase 1

完成：

```text
Electron
React
WebContentsView
FastAPI
WebSocket
```

客户端可以：

- 加载网页

- 手动浏览

- Python 与 Electron 通信

- Pause / Resume

---

## Phase 2

完成：

```text
BrowserController
Browser RPC
Platform Adapter
```

可以：

```text
Python
↓
Electron
↓
读取当前网页数据
```

---

## Phase 3

实现：

```text
Job Parser

Job Model

Rule Engine

JobAgent
```

浏览职位时：

右侧 Agent Panel 可以显示：

```text
岗位信息

匹配度

优点

风险

建议
```

---

## Phase 4

实现：

```text
JobDiscoveryGraph

ApplicationGraph
```

可以：

```text
获取职位
↓

Agent 分析

↓

用户确认

↓

投递
```

先使用 Copilot 模式。

---

## Phase 5

实现：

```text
Chat Parser

ChatAgent

ChatGraph

LangGraph interrupt
```

支持：

```text
读取 HR 消息

↓

生成回复

↓

Approval Card

↓

用户确认

↓

发送
```

---

## Phase 6

增加：

```text
Auto mode

Settings

History

Job Dashboard

Chat Dashboard

Tracing

Error Recovery
```

---

# 四十二、第一版不要实现的东西

不要：

```text
Kafka

微服务

Kubernetes

Redis Cluster

Celery 集群

复杂 RAG

向量数据库

多用户系统

账号系统

云部署

OCR

Computer Vision

复杂 Multi Agent

20 个 Agent

完全自主 Browser Agent
```

这是一个本地单用户桌面应用。

保持架构清晰。

---

# 四十三、项目目录

建议：

```text
job-agent/
│
├── client/
│   │
│   ├── electron/
│   │   ├── main.ts
│   │   ├── preload.ts
│   │   │
│   │   ├── browser/
│   │   │   ├── BrowserController.ts
│   │   │   ├── ElectronBrowserController.ts
│   │   │   ├── BrowserManager.ts
│   │   │   └── SessionManager.ts
│   │   │
│   │   ├── ipc/
│   │   │
│   │   └── websocket/
│   │
│   ├── src/
│   │   ├── App.tsx
│   │   │
│   │   ├── pages/
│   │   │   ├── BrowserPage.tsx
│   │   │   ├── JobsPage.tsx
│   │   │   ├── ChatPage.tsx
│   │   │   ├── ReviewPage.tsx
│   │   │   ├── HistoryPage.tsx
│   │   │   └── SettingsPage.tsx
│   │   │
│   │   ├── components/
│   │   │   ├── AgentPanel/
│   │   │   ├── ApprovalCard/
│   │   │   ├── BrowserToolbar/
│   │   │   ├── JobAnalysis/
│   │   │   └── StatusBar/
│   │   │
│   │   ├── stores/
│   │   ├── hooks/
│   │   ├── services/
│   │   └── types/
│   │
│   └── package.json
│
│
├── backend/
│   │
│   ├── pyproject.toml
│   │
│   ├── app/
│   │   ├── main.py
│   │   │
│   │   ├── api/
│   │   │
│   │   ├── websocket/
│   │   │
│   │   ├── browser/
│   │   │   ├── client.py
│   │   │   └── protocol.py
│   │   │
│   │   ├── agents/
│   │   │   ├── job_agent.py
│   │   │   └── chat_agent.py
│   │   │
│   │   ├── graphs/
│   │   │   ├── discovery_graph.py
│   │   │   ├── application_graph.py
│   │   │   └── chat_graph.py
│   │   │
│   │   ├── platforms/
│   │   │   ├── base.py
│   │   │   │
│   │   │   └── platform_a/
│   │   │       ├── adapter.py
│   │   │       ├── selectors.py
│   │   │       ├── parser.py
│   │   │       └── models.py
│   │   │
│   │   ├── rules/
│   │   │   ├── job_rules.py
│   │   │   └── chat_policy.py
│   │   │
│   │   ├── models/
│   │   │
│   │   ├── database/
│   │   │   ├── models.py
│   │   │   ├── repository.py
│   │   │   └── session.py
│   │   │
│   │   ├── config/
│   │   │
│   │   └── services/
│   │
│   └── tests/
│
├── docs/
│   ├── architecture.md
│   ├── browser-rpc.md
│   ├── agent-design.md
│   └── development.md
│
├── scripts/
│
├── .gitignore
│
└── README.md
```

---

# 四十四、Mock Platform

由于真实招聘平台 DOM 可能随时变化，第一阶段不要因为实际 Selector 无法确定而阻塞整个项目。

同时实现：

```text
MockRecruitmentPlatform
```

提供本地 Mock HTML 页面：

```text
mock-site/
```

包含：

```text
职位列表

职位详情

聊天列表

聊天详情

HR 新消息

立即沟通按钮
```

这样整个 Agent 流程可以完全在 Mock 网站验证。

真实招聘平台 Adapter 作为独立模块开发。

---

# 四十五、测试要求

至少测试：

## Rule Engine

```text
location filter

salary filter

blacklist

job type
```

---

## JobAgent Structured Output

验证永远得到符合 Schema 的结构。

---

## ChatAgent

测试：

```text
问候

毕业时间

面试时间

薪资

offer
```

risk 是否正确。

---

## LangGraph

测试：

```text
AUTO

CONFIRM

MANUAL
```

三个分支。

---

## Interrupt

必须验证：

```text
interrupt
↓

resume
↓

不会重复发送
```

---

## Browser RPC

测试：

```text
request

response

timeout

error

disconnect
```

---

# 四十六、README

必须写完整 README。

包括：

```text
项目简介

架构

依赖

安装步骤

运行后端

运行客户端

配置 API Key

开发模式

Mock Platform

测试

目录结构

安全注意事项
```

必须保证新开发者按照 README 可以运行项目。

---

# 四十七、启动方式

目标：

根目录提供统一脚本：

Windows：

```text
scripts/dev.ps1
```

执行后：

```text
启动 FastAPI

↓

等待 Backend Ready

↓

启动 Electron
```

另外提供：

```text
scripts/test.ps1
```

---

# 四十八、验收标准

第一阶段完整项目至少满足：

桌面客户端可以启动。

客户端中间可以打开招聘网站 / Mock Site。

用户可以在客户端中正常操作网页。

Electron 与 Python 后端 WebSocket 双向通信正常。

Python 可以通过 Browser RPC 请求读取当前网页信息。

JobAgent 能分析当前职位。

结果实时显示在右侧 Agent Panel。

可以点击：

```text
Apply

Skip
```

ChatAgent 能读取 HR Mock Message。

能够生成结构化回复。

CONFIRM 消息触发 LangGraph interrupt。

客户端展示 ApprovalCard。

确认后 Graph 恢复。

消息发送一次且仅一次。

Pause 后 Agent 不能操作网页。

Resume 后 Agent 重新读取当前页面。

所有主要行为写入数据库。

所有异常有日志。

API Key 不进入 Git。

项目有完整 README。

---

# 四十九、编码原则

必须遵循：

```text
类型安全

模块化

低耦合

明确职责

依赖倒置

Adapter Pattern

Service Layer

Structured Output

State Machine

Event-driven UI
```

避免：

```text
God Class

全局变量

到处直接操作 DOM

到处直接调用 LLM

大量字符串状态

Agent 直接控制 Selector

复杂无意义抽象
```

---

# 五十、开发方式

请不要一次生成大量无法运行的伪代码。

按照阶段真正实现。

每完成一个阶段：

1. 保证可以运行。

2. 补充测试。

3. 修复 TypeScript / Python 类型错误。

4. 确保 lint 通过。

5. 确保前后端通信正常。

6. 更新 README。

7. 再进入下一阶段。

遇到不确定的真实招聘网站 Selector：

不要编造。

使用：

```text
TODO
+
Mock Platform
+
Selector configuration
```

让整个架构仍然能够运行。

---

# 五十一、实现优先级

最高优先级：

```text
Electron Browser

Browser RPC

Agent Panel

LangGraph

Human-in-the-loop

Pause / Resume
```

其次：

```text
Job analysis

Chat analysis

Database

Settings
```

最后：

```text
UI polish

多平台

高级统计
```

---

# 五十二、最终产品体验

用户打开 Job Agent。

客户端恢复之前的浏览器 Session。

用户进入招聘网站。

打开一个职位。

Agent 自动读取页面。

右侧立即显示：

```text
岗位匹配度：86

推荐：APPLY

优势：

Go
Redis
MySQL

风险：

工作经验要求偏高
```

用户可以自己投递，也可以交给 Agent。

HR 发消息：

```text
明天下午方便面试吗？
```

Agent 自动生成：

```text
您好，明天下午三点以后都有时间，请问具体几点方便？
```

因为：

```text
risk = CONFIRM
```

客户端右侧弹出：

```text
[发送]

[编辑]

[拒绝]
```

用户确认。

LangGraph 恢复。

消息发送。

之后 Graph 进入：

```text
WAITING_REPLY
```

整个过程中：

用户始终可以：

```text
Pause Agent
```

并自己接管浏览器。

这就是第一版最终需要实现的产品。
