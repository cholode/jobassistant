# 开发环境

当前项目已根据《项目设计.md》准备开发依赖，并完成 Phase 1 桌面浏览器与后端通信。启动和测试方法见 [README](README.md)。

## 本机工具

- Windows 11 x64
- Node.js 24.20.0
- pnpm 11.25.0
- Git 2.55.0
- uv 0.12.10
- Python 3.12.14，通过 uv 管理；项目解释器位于 `backend/.venv/Scripts/python.exe`

前五项是本机已有工具。Python 和前后端项目依赖在此次配置中安装。

本机 uv 的全局 `python3.12` 快捷入口存在目录链接异常，自动审批检查阻止了目录链接修复。项目虚拟环境已使用真实解释器创建并验证通过；日常请在 `backend` 下运行 `uv run python`，或直接使用上述项目解释器。

## 项目依赖

后端使用独立虚拟环境 `backend/.venv`：FastAPI、Uvicorn/WebSocket、LangChain、LangChain OpenAI 适配器、LangGraph、SQLite checkpoint、Pydantic、SQLAlchemy、aiosqlite、structlog，以及 pytest、pytest-asyncio 和 Ruff。

客户端使用 `client/node_modules`：Electron 44.2.0、React、TypeScript、Vite、Tailwind CSS、Zustand、TanStack Query、Radix UI、shadcn CLI 及相关样式工具。Electron 使用本机已有官方缓存安装，并固定了版本。

Playwright 已安装，并已通过本机 Microsoft Edge 153.0.4234.48 的无头浏览器测试。独立 Chromium / Headless Shell 下载因官方源超时未完成，已停止下载；当前测试使用 `chromium.launch({ channel: 'msedge', headless: true })`，不依赖该下载。

shadcn/ui 的组件需要在实现界面时按需生成；当前安装其 CLI 和基础依赖。Tailwind/Vite 的应用配置也将在实现客户端时添加。

Python 自带 SQLite，不需要独立数据库服务。第一版无需安装 Redis、MySQL、Docker 服务、CUDA 或 Visual Studio C++ 编译工具。

## 重新安装依赖

在项目根目录的 PowerShell 中执行：

```powershell
uv python install 3.12
Set-Location backend
uv sync --locked
Set-Location ../client
pnpm.cmd install --frozen-lockfile
Set-Location ..
```

`backend/uv.lock` 和 `client/pnpm-lock.yaml` 固定了本次安装的依赖版本。Electron 安装脚本只在客户端项目内允许运行。

本机已有 Edge，无需额外安装浏览器。如后续希望使用 Playwright 配套版本，可在 `client` 下运行 `pnpm.cmd exec playwright install chromium`。正式客户端已使用 Electron 内置的 Chromium。

## 验证环境

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\check-env.ps1
```

检查会验证后端依赖导入、FastAPI HTTP、SQLite、LangGraph，以及 Playwright 浏览器和 Electron WebContentsView 的真实启动；不会调用付费模型 API，也不会访问招聘网站。浏览器检查优先使用已安装的 Playwright Chromium，否则使用本机 Edge。

2026-09-25：上述统一检查全部通过，`uv pip check` 依赖兼容性检查通过，Python 检查脚本通过 Ruff 检查。

## 后续需要提供

- 模型服务的 API Key、模型名称；如果使用兼容服务，还需要服务地址。在实现配置读取后使用 `backend/.env` 或系统安全存储，不要写入代码。
- LangSmith API Key 是可选项。
- 真实招聘网站账号应在未来客户端内手动登录；开发 Mock 平台不需要账号。

`backend/.env.example` 提供变量模板，`.gitignore` 已排除密钥、浏览器登录资料、数据库及日志。

当前可运行 `powershell -ExecutionPolicy Bypass -File .\scripts\dev.ps1` 启动 Phase 1。职位分析、投递和 Agent 聊天功能仍待后续阶段开发。

版本要求参考：[Vite 安装要求](https://vite.dev/guide/)、[Electron 安装说明](https://www.electronjs.org/docs/latest/tutorial/installation)。
